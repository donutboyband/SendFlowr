{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sendflowr.ai/schemas/timing-decision.schema.json",
  "title": "SendFlowr Timing Decision",
  "description": "Canonical output of the SendFlowr Timing Layer. Represents a precise, latency-aware send trigger decision.",
  "type": "object",
  "required": [
    "decision_id",
    "universal_user_id",
    "target_minute_utc",
    "trigger_timestamp_utc",
    "confidence_score",
    "latency_estimate_seconds",
    "model_version",
    "explanation_ref",
    "created_at_utc"
  ],
  "properties": {
    "decision_id": {
      "type": "string",
      "description": "Globally unique identifier for this timing decision (UUID or ULID)."
    },
    "universal_user_id": {
      "type": "string",
      "description": "SendFlowr universal user identifier resolved via identity contract."
    },
    "target_minute_utc": {
      "type": "integer",
      "minimum": 0,
      "maximum": 10079,
      "description": "Canonical minute slot (0–10079) representing intended inbox arrival time in UTC."
    },
    "trigger_timestamp_utc": {
      "type": "string",
      "format": "date-time",
      "description": "Exact UTC timestamp when the send trigger must be fired to compensate for latency."
    },
    "latency_estimate_seconds": {
      "type": "number",
      "minimum": 0,
      "description": "Observed or predicted ESP processing latency used to compute trigger time."
    },
    "confidence_score": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Normalized confidence in the timing decision, derived from curve sharpness and signal strength."
    },
    "model_version": {
      "type": "string",
      "description": "Version identifier of the timing model used to generate this decision."
    },
    "explanation_ref": {
      "type": "string",
      "description": "Opaque reference key to an explainability record or audit log entry."
    },
    "created_at_utc": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the timing decision was generated."
    },
    "debug": {
      "type": "object",
      "description": "Optional debug payload for internal inspection and glass-box UI.",
      "required": [],
      "properties": {
        "base_curve_peak_minute": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10079,
          "description": "Minute slot where the base probability curve peaks."
        },
        "applied_weights": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["signal", "weight"],
            "properties": {
              "signal": {
                "type": "string",
                "description": "Name of contextual signal applied (e.g., shopify_browse, support_ticket)."
              },
              "weight": {
                "type": "number",
                "description": "Weight applied to the base curve for this signal."
              }
            }
          }
        },
        "suppressed": {
          "type": "boolean",
          "description": "Indicates whether a circuit breaker forced propensity to zero."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
